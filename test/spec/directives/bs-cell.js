'use strict';

describe('Directive: bsCell', function () {

  // load the directive's module
  beforeEach(module('battleshipApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

  it('should probably have some tests')
});
